# External support modules for building inside motorFastech
# Define path to the module itself

FASTECHMOTOR = $(TOP)/../..

# Include environment files from Motor, motorFastech and the IOC itself

-include $(TOP)/../../../RELEASE.$(EPICS_HOST_ARCH).local
-include $(TOP)/../../configure/RELEASE
-include $(TOP)/../../configure/RELEASE.local
-include $(TOP)/configure/RELEASE.local
